import { Inter, Bangers, Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const bangers = Bangers({ subsets: ['latin'], weight: '400' });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });